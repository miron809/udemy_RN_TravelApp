import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 32,
  },
  subtitle: {
    color: 'black',
    fontSize: 20,
    marginTop: 40,
    marginBottom: 34,
  },
});

export default styles;
